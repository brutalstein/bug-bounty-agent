# Required local secrets and operator-specific values.
# Copy this file to `.env` and populate the values before running the CLI.

BB_AIRTABLE_STAGING_API_KEY=replace_with_your_airtable_staging_api_key
BB_AIRTABLE_STAGING_ACCOUNT_EMAIL=staging-api-key-operator
